# Server configuration
server.port=8080

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/votingsystem
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Keycloak configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8180/realms/voting-system
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8180/realms/voting-system/protocol/openid-connect/certs

# Keycloak admin credentials
keycloak.realm=voting-system
keycloak.auth-server-url=http://localhost:8180
keycloak.resource=voting-app
keycloak.credentials.secret=IHsA1hamQwzEBLaoAo2RZJQ58FfthnQw
keycloak.use-resource-role-mappings=true
keycloak.bearer-only=true